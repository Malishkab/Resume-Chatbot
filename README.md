# Resume Chatbot

An AI chatbot that answers interview questions as a job candidate, based on their resume.

- `backend/` — FastAPI + Groq, parses a resume PDF and answers questions about it.
- `frontend/` — React (Vite) chat interface.

See each folder's own setup instructions.